export const Modal = () => {
    return (
        <div className="overlay">
            <div className="modal">
                <img src={`${asd}`} alt={`${asd}`} />
            </div>
        </div>
    )
}
