export const Modal = () => {
    return (
        <div class="overlay">
            <div class="modal">
                <img src={`${asd}`} alt={`${asd}`} />
            </div>
        </div>
    )
}
